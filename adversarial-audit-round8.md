# Novel Forge 第八轮剩余事项修复与对抗性审查

## 修复内容

本轮针对上一轮遗留的可行动事项进行了修复。未登录入口现在会区分加载状态和认证失败状态；当 OAuth 会话失效或验证未完成时，页面显示明确的“登录验证未完成或会话已失效”提示，并提供“重新登录”按钮。该提示不暴露上游认证内部错误，也不尝试删除或绕过 CAPTCHA。

前端 AI 和项目 mutation 统一使用安全错误提示。AI provider 失败会显示“AI 暂时无法生成，请稍后重试”，并明确当前编辑内容没有被覆盖；持久化配额或 activeUntil 锁冲突会提示项目正在生成或请求过于频繁；未授权状态会提示重新登录。原始内部错误码不再直接作为用户提示的首选文案。

generation_usage 测试新增共享数据库行的并发语义：两个独立调用同时尝试保留同一 user/project 锁时，只允许一个调用成功，第二个调用因 activeUntil 条件更新影响行数为零而失败。这是对跨实例原子更新语义的确定性模拟，未伪造真实多实例环境。

## 验证结果

| 项目 | 结果 |
|---|---|
| 定向回归测试 | 4 个测试文件、18 个测试通过 |
| 完整 Vitest | 20 个测试文件、60 个测试通过 |
| TypeScript 检查 | 通过 |
| 生产构建 | 通过，NODE_ENV=production |
| 认证反馈契约 | 通过，含重新登录和失败提示断言 |
| 共享锁并发语义 | 通过，一成功一失败 |
| 真实登录后 AI 生成 | 未实测，沙箱 CAPTCHA 阻塞 |
| 真实 staging 多实例压测 | 未执行，需要可控 staging 数据库和多实例环境 |

## 对抗性结论

未发现新增的跨用户归属、未保护 mutation、配额绕过、锁释放遗漏、危险动态执行或敏感错误透传路径。登录 CAPTCHA 属于上游认证安全机制，不能通过应用代码删除或绕过；本轮改进的是失败可见性和重新登录入口。多实例测试已加强为共享行原子锁的并发契约测试，但真实网络、连接池和数据库隔离级别仍应在 staging 进行最终验收。
