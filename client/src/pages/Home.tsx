import React, { useEffect, useMemo, useState } from "react";
import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { BookOpen, ChevronRight, Clock3, FileDown, FolderPlus, Loader2, Plus, Sparkles, Trash2, Wand2 } from "lucide-react";
import { toast } from "sonner";
import { currentHash, readNavTarget, targetAnchor, targetToWorkspaceTab } from "@/lib/navigation";
import { StandaloneModulePanel, StandaloneTrendPanel } from "@/components/StandaloneModules";
import { TrendTable } from "@/components/TrendTable";
import { GenrePicker } from "@/components/GenrePicker";
import { SynopsisFields } from "@/components/SynopsisFields";
import { canSaveProject } from "@shared/projectValidation";
import { buildProjectCreateInput } from "@shared/projectForm";
import { canGenerateOutline, normalizeStoryDirection } from "@shared/storyDirection";

const emptyProject = { title: "", genre: "", synopsis: "", targetWords: 100000 };
const emptyDocs = { outline: "", worldSetting: "", characters: "", conflicts: "", styleGuide: "克制、细腻、具有连续追读钩子的中文网文叙事。" };

export default function Home() {
  const utils = trpc.useUtils();
  const projects = trpc.projects.list.useQuery(); const trends = trpc.trends.list.useQuery(); const notifications = trpc.notifications.list.useQuery();
  const [selectedId, setSelectedId] = useState<number | null>(null); const [selectedNotification, setSelectedNotification] = useState<any>(null); const [projectForm, setProjectForm] = useState(emptyProject); const [synopsisIdea, setSynopsisIdea] = useState(""); const [showNew, setShowNew] = useState(false); const [navigationTarget, setNavigationTarget] = useState(() => readNavTarget(currentHash()));
  useEffect(() => { if (!selectedId && projects.data?.[0]) setSelectedId(projects.data[0].id); }, [projects.data, selectedId]);
  useEffect(() => { if (typeof window === "undefined") return; const onHashChange = () => { const target = readNavTarget(currentHash()); setNavigationTarget(target); const anchor = targetAnchor(target); if (anchor && typeof document !== "undefined") window.setTimeout(() => document.getElementById(anchor)?.scrollIntoView({ behavior: "smooth", block: "start" }), 0); }; window.addEventListener("hashchange", onHashChange); return () => window.removeEventListener("hashchange", onHashChange); }, []);
  const selected = projects.data?.find(p => p.id === selectedId) ?? null;
  const workspace = trpc.workspace.get.useQuery({ projectId: selectedId ?? 0 }, { enabled: Boolean(selectedId) });
  const create = trpc.projects.create.useMutation({ onSuccess: () => { utils.projects.list.invalidate(); setShowNew(false); setProjectForm(emptyProject); setSynopsisIdea(""); toast.success("项目已建立"); }, onError: error => toast.error(error.message) });
  const optimizeSynopsis = trpc.projects.optimizeSynopsis.useMutation({ onSuccess: value => { setProjectForm(current => ({ ...current, synopsis: value })); toast.success("简介已优化，可继续修改"); }, onError: error => toast.error(error.message) });
  const remove = trpc.projects.remove.useMutation({ onSuccess: () => { setSelectedId(null); utils.projects.list.invalidate(); toast.success("项目已移除"); }, onError: error => toast.error(error.message) });
  const markNotification = trpc.notifications.markRead.useMutation({ onSuccess: () => utils.notifications.list.invalidate(), onError: error => toast.error(error.message) });
  const unread = notifications.data?.filter(n => !n.readAt).length ?? 0;
  const wordCount = useMemo(() => workspace.data?.chapters.reduce((sum, c) => sum + c.body.length, 0) ?? 0, [workspace.data]);

  return <div className="min-h-[calc(100vh-4rem)] editorial-grid px-5 py-8 md:px-10 md:py-12"><div className="mx-auto max-w-[1480px]">
    <header id="novel-forge-overview" className="scroll-mt-20 grid gap-8 md:grid-cols-[1.15fr_.85fr] items-end mb-12 motion-rise"><div><div className="mb-5 flex flex-wrap items-center gap-3"><p className="text-[11px] uppercase tracking-[.35em] text-muted-foreground">Novel Forge · 创作者控制台</p><span className="text-[10px] uppercase tracking-[.18em] text-accent border-b border-accent/40 pb-1">OH Story method pack · v0.7.5</span></div><h1 className="font-display text-[clamp(3.6rem,8vw,8.5rem)] leading-[.88] tracking-[-.06em]">写作，<br /><em className="font-normal">更接近作品。</em></h1></div><div className="md:pb-2 max-w-md"><p className="text-lg leading-8 text-muted-foreground">把灵感、结构与持续产出放进同一间数字编辑室。AI 负责推进，你负责判断。</p><div className="mt-7 flex items-center gap-6 text-xs uppercase tracking-[.2em]"><span>{projects.data?.length ?? 0} 个项目</span><span className="h-px w-12 bg-foreground/30" /><span>{unread} 条未读通知</span></div>{notifications.data?.length ? <div className="mt-6 border-l-2 border-accent pl-4 space-y-2">{notifications.data.slice(0, 2).map(n => <button key={n.id} onClick={() => { setSelectedNotification(n); if (n.projectId) setSelectedId(n.projectId); markNotification.mutate({ id: n.id }); }} className="block text-left text-xs text-muted-foreground hover:text-foreground"><span className="text-foreground">{n.title}</span> · {n.message}{!n.readAt && <span className="ml-2 text-accent">未读</span>}</button>)}</div> : null}{selectedNotification && <Card className="mt-5 rounded-none"><CardContent className="p-4"><div className="flex items-start justify-between gap-4"><div><p className="text-xs uppercase tracking-[.2em] text-muted-foreground">通知详情</p><p className="font-display text-lg mt-2">{selectedNotification.title}</p><p className="text-sm text-muted-foreground mt-2 leading-6">{selectedNotification.message}</p></div><button className="text-xs text-muted-foreground" onClick={() => setSelectedNotification(null)}>关闭</button></div>{selectedNotification.projectId && <p className="text-xs text-accent mt-3">已定位到关联小说项目</p>}</CardContent></Card>}<div className="mt-7 grid grid-cols-2 gap-3"><div className="editorial-note"><span className="editorial-note__eyebrow">WRITING RHYTHM</span><strong>先情绪，后结构。</strong><span>每一章都留下可追读的推进力。</span></div><div className="editorial-note editorial-note--accent"><span className="editorial-note__eyebrow">METHOD PACK</span><strong>OH Story · 0.7.5</strong><span>情绪目标、连续性与趋势验证。</span></div></div></div></header>
    <section id="novel-forge-workspace" className="scroll-mt-20 grid gap-6 xl:grid-cols-[300px_1fr] items-start"><aside className="space-y-4"><div className="flex items-center justify-between"><div><p className="text-[10px] uppercase tracking-[.3em] text-muted-foreground">Your projects</p><h2 className="font-display text-2xl mt-1">小说项目</h2></div><Button size="icon" variant="outline" className="rounded-none" onClick={() => setShowNew(true)}><FolderPlus className="h-4 w-4" /></Button></div><div className="space-y-2">{projects.isLoading ? <Loader2 className="h-5 w-5 animate-spin" /> : projects.data?.length ? projects.data.map(project => <button key={project.id} onClick={() => setSelectedId(project.id)} className={`w-full text-left p-4 border transition-all ${selectedId === project.id ? "bg-[#171613] text-[#f5f1e9] border-[#171613]" : "bg-background/50 hover:bg-card"}`}><div className="flex items-start justify-between gap-3"><span className="font-display text-lg leading-tight">{project.title}</span><ChevronRight className="h-4 w-4 shrink-0 mt-1" /></div><div className={`mt-5 flex justify-between text-[10px] uppercase tracking-[.18em] ${selectedId === project.id ? "text-[#f5f1e9]/60" : "text-muted-foreground"}`}><span>{project.genre}</span><span>{project.status}</span></div></button>) : <div className="border border-dashed p-6 text-sm text-muted-foreground leading-6">还没有项目。先建立一部小说，让这间编辑室有第一份档案。</div>}</div></aside>
    <main className="min-w-0">{showNew && <Card className="rounded-none border-foreground/20 mb-6 motion-rise"><CardHeader><CardTitle className="font-display text-2xl">建立新的作品档案</CardTitle></CardHeader><CardContent className="grid gap-4 md:grid-cols-2"><Input placeholder="书名" value={projectForm.title} onChange={e => setProjectForm({ ...projectForm, title: e.target.value })} /><GenrePicker value={projectForm.genre} onChange={genre => setProjectForm({ ...projectForm, genre })} /><Input type="number" placeholder="目标字数" value={projectForm.targetWords} onChange={e => setProjectForm({ ...projectForm, targetWords: Number(e.target.value) })} /><SynopsisFields idea={synopsisIdea} synopsis={projectForm.synopsis} onIdeaChange={setSynopsisIdea} onSynopsisChange={synopsis => setProjectForm({ ...projectForm, synopsis })} /><div className="md:col-span-2 flex flex-wrap items-center gap-3"><Button type="button" variant="outline" className="rounded-none" disabled={!synopsisIdea.trim() || optimizeSynopsis.isPending} onClick={() => optimizeSynopsis.mutate({ title: projectForm.title, genre: projectForm.genre, idea: synopsisIdea })}>{optimizeSynopsis.isPending && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}AI 优化简介</Button><span className="text-xs text-muted-foreground">先输入粗略想法，再由 AI 整理成可发布简介。</span></div><div className="md:col-span-2 flex gap-3"><Button className="rounded-none" disabled={!canSaveProject(projectForm.title, projectForm.genre) || create.isPending} onClick={() => create.mutate(buildProjectCreateInput(projectForm))}>{create.isPending && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}保存项目</Button><Button variant="ghost" className="rounded-none" onClick={() => setShowNew(false)}>取消</Button></div></CardContent></Card>}
      {!selected ? (navigationTarget === "trends" ? <StandaloneTrendPanel trends={trends.data ?? []} /> : <StandaloneModulePanel target={navigationTarget} onCreate={() => setShowNew(true)} />) : <ProjectWorkspace project={selected} workspace={workspace.data} trends={trends.data ?? []} wordCount={wordCount} navigationTarget={navigationTarget} onDelete={() => remove.mutate({ id: selected.id })} />}
    </main></section>
  </div></div>;
}

function EmptyState({ onCreate }: { onCreate: () => void }) { return <div className="empty-manuscript min-h-[420px] border border-dashed border-foreground/25 p-6 md:p-10"><div className="empty-manuscript__top"><span className="folio-stamp">NF</span><span className="font-mono text-[10px] tracking-[.18em] text-muted-foreground">ARCHIVE / 000</span><span className="empty-manuscript__rule" /></div><div className="empty-manuscript__body"><div className="empty-manuscript__index"><span>EDITORIAL NOTE</span><b>01</b><i>—</i><small>New manuscript</small></div><div className="text-center"><BookOpen className="mx-auto h-9 w-9 mb-5" strokeWidth={1.5} /><h2 className="font-display text-3xl md:text-4xl mb-3">选择一部作品开始工作</h2><p className="max-w-md text-muted-foreground leading-7 mb-7">从一个书名和一句故事抓手开始，世界观、人物、章节和版本会在这里逐步成形。</p><Button onClick={onCreate} className="rounded-none"><Plus className="mr-2 h-4 w-4" />建立首个项目</Button></div></div><div className="empty-manuscript__footer"><span>Novel Forge Editorial Studio</span><span>AI 负责推进 · 作者负责判断</span></div></div> }

function ProjectWorkspace({ project, workspace, trends, wordCount, navigationTarget, onDelete }: { project: any; workspace: any; trends: any[]; wordCount: number; navigationTarget: string; onDelete: () => void }) {
  const utils = trpc.useUtils();
  const updateProject = trpc.projects.update.useMutation({ onSuccess: () => { utils.projects.list.invalidate(); utils.workspace.get.invalidate({ projectId: project.id }); toast.success("项目资料已更新"); }, onError: error => toast.error(error.message) });
  const [editingProject, setEditingProject] = useState(false);
  const [projectDraft, setProjectDraft] = useState({ title: project.title, genre: project.genre, synopsis: project.synopsis, targetWords: project.targetWords });
  const [direction, setDirection] = useState(""); const [outline, setOutline] = useState(""); const [activeChapter, setActiveChapter] = useState<any>(null); const [style, setStyle] = useState("克制、细腻、具有连续追读钩子的中文网文叙事。"); const [activeTab, setActiveTab] = useState("outline");
  useEffect(() => { setActiveTab(targetToWorkspaceTab(readNavTarget(`#${navigationTarget}`))); }, [navigationTarget]);
  const generateOutline = trpc.workspace.generateOutline.useMutation({ onSuccess: value => { setOutline(value); saveVersion.mutate({ projectId: project.id, entityType: "outline", entityId: 0, label: "AI 大纲生成", content: value }); toast.success("章节大纲已生成，可继续编辑"); }, onError: error => toast.error(error.message) });
  const handleGenerateOutline = () => { const cleanedDirection = normalizeStoryDirection(direction); if (!canGenerateOutline(cleanedDirection)) { toast.error("请先填写故事方向，再生成章节大纲"); return; } generateOutline.mutate({ projectId: project.id, direction: cleanedDirection, chapterCount: 30 }); };
  const saveVersion = trpc.workspace.saveVersion.useMutation({ onSuccess: () => toast.success("版本已归档") });
  const schedules = trpc.schedules.list.useQuery();
  const scheduleCreate = trpc.schedules.create.useMutation({ onSuccess: () => { schedules.refetch(); toast.success("自动续写计划已创建"); }, onError: error => toast.error(error.message) });
  const scheduleSetEnabled = trpc.schedules.setEnabled.useMutation({ onSuccess: () => { schedules.refetch(); toast.success("续写计划状态已更新"); }, onError: error => toast.error(error.message) });
  const scheduleRemove = trpc.schedules.remove.useMutation({ onSuccess: () => { schedules.refetch(); toast.success("续写计划已删除"); }, onError: error => toast.error(error.message) });
  const createTrend = trpc.trends.create.useMutation({ onSuccess: () => { utils.trends.list.invalidate(); toast.success("趋势标签已加入"); }, onError: error => toast.error(error.message) });
  const removeTrend = trpc.trends.remove.useMutation({ onSuccess: () => { utils.trends.list.invalidate(); toast.success("趋势标签已删除"); }, onError: error => toast.error(error.message) });
  const updateTrend = trpc.trends.update.useMutation({ onSuccess: () => { utils.trends.list.invalidate(); toast.success("趋势标签已更新"); }, onError: error => toast.error(error.message) });
  const [newTrend, setNewTrend] = useState({ label: "", category: "", heat: 70, note: "" });
  const [cronExpression, setCronExpression] = useState("0 0 22 * * *");
  const saveChapter = trpc.workspace.saveChapter.useMutation({ onSuccess: () => { utils.workspace.get.invalidate({ projectId: project.id }); utils.workspace.versions.invalidate(); toast.success("章节已保存"); }, onError: error => toast.error(error.message) });
  const generateChapter = trpc.workspace.generateChapter.useMutation({ onSuccess: value => { const chapter = value.at(-1); setActiveChapter(chapter); if (chapter) saveVersion.mutate({ projectId: project.id, entityType: "chapter", entityId: chapter.id, label: `第${chapter.chapterNumber}章生成`, content: chapter.body }); toast.success("章节正文已生成"); } });
  const saveDocs = trpc.workspace.saveDocs.useMutation({ onSuccess: () => { utils.workspace.get.invalidate({ projectId: project.id }); toast.success("策划文档已保存"); }, onError: error => toast.error(error.message) });
  const [docs, setDocs] = useState({ ...emptyDocs, ...(workspace?.docs ?? {}) });
  useEffect(() => { if (workspace?.docs) { setDocs({ ...emptyDocs, ...workspace.docs }); setOutline(workspace.docs.outline ?? ""); } }, [workspace?.docs]);
  const chapters = workspace?.chapters ?? [];
  const current = activeChapter ?? chapters[0];
  const versionEntityType = current ? "chapter" as const : "outline" as const;
  const versions = trpc.workspace.versions.useQuery({ projectId: project.id, entityType: versionEntityType, entityId: current?.id ?? 0 }, { enabled: Boolean(project.id) });
  const [selectedVersion, setSelectedVersion] = useState<any>(null);
  const [compareVersion, setCompareVersion] = useState<any>(null);
  const rollbackVersion = trpc.workspace.rollbackVersion.useMutation({ onSuccess: () => { utils.workspace.get.invalidate({ projectId: project.id }); versions.refetch(); toast.success("版本已持久化回滚"); }, onError: error => toast.error(error.message) });
  const exportText = () => { const text = `${project.title}\n\n${project.synopsis}\n\n【全书大纲】\n${outline}\n\n${chapters.map((c: any) => `第${c.chapterNumber}章 ${c.title}\n\n${c.body}`).join("\n\n")}`; const blob = new Blob([text], { type: "text/plain;charset=utf-8" }); const a = document.createElement("a"); a.href = URL.createObjectURL(blob); a.download = `${project.title}.txt`; a.click(); URL.revokeObjectURL(a.href); };
  const exportMarkdown = () => { const text = `# ${project.title}\n\n${project.synopsis}\n\n## 全书大纲\n\n${outline}\n\n${chapters.map((c: any) => `## 第${c.chapterNumber}章 ${c.title}\n\n${c.body}`).join("\n\n")}`; const blob = new Blob([text], { type: "text/markdown;charset=utf-8" }); const a = document.createElement("a"); a.href = URL.createObjectURL(blob); a.download = `${project.title}.md`; a.click(); URL.revokeObjectURL(a.href); };
  return <div className="space-y-6 motion-rise">{editingProject && <Card className="rounded-none border-foreground/20"><CardHeader><CardTitle className="font-display text-2xl">编辑项目资料</CardTitle></CardHeader><CardContent className="grid gap-3 md:grid-cols-2"><Input value={projectDraft.title} onChange={e => setProjectDraft({ ...projectDraft, title: e.target.value })} placeholder="书名" /><GenrePicker value={projectDraft.genre} onChange={genre => setProjectDraft({ ...projectDraft, genre })} /><Input type="number" value={projectDraft.targetWords} onChange={e => setProjectDraft({ ...projectDraft, targetWords: Number(e.target.value) })} placeholder="目标字数" /><Textarea className="md:col-span-2" value={projectDraft.synopsis} onChange={e => setProjectDraft({ ...projectDraft, synopsis: e.target.value })} placeholder="简介" /><div className="flex gap-3 md:col-span-2"><Button className="rounded-none" onClick={() => { updateProject.mutate({ id: project.id, ...projectDraft }); setEditingProject(false); }}>保存项目</Button><Button variant="ghost" className="rounded-none" onClick={() => setEditingProject(false)}>取消</Button></div></CardContent></Card>}<div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between border-b border-foreground/20 pb-6"><div><div className="flex gap-2 mb-4"><Badge variant="outline" className="rounded-none uppercase tracking-[.15em] text-[10px]">{project.genre}</Badge><Badge variant="outline" className="rounded-none uppercase tracking-[.15em] text-[10px]">{project.status}</Badge></div><h2 className="font-display text-5xl md:text-6xl leading-none">{project.title}</h2><p className="text-muted-foreground mt-4 max-w-2xl leading-7">{project.synopsis || "尚未填写作品简介。"}</p></div><div className="flex gap-2"><Button variant="outline" className="rounded-none" onClick={exportText}><FileDown className="mr-2 h-4 w-4" />TXT</Button><Button variant="outline" className="rounded-none" onClick={exportMarkdown}>Markdown</Button><Button variant="outline" className="rounded-none" onClick={() => setEditingProject(true)}>编辑项目</Button><Button variant="ghost" className="rounded-none text-destructive" onClick={onDelete}><Trash2 className="mr-2 h-4 w-4" />删除</Button></div></div><div className="grid grid-cols-2 md:grid-cols-4 gap-px bg-foreground/15 border border-foreground/15"><Metric label="目标字数" value={`${Math.round(project.targetWords / 10000)} 万`} /><Metric label="当前正文" value={`${wordCount.toLocaleString()} 字`} /><Metric label="章节数" value={`${chapters.length} 章`} /><Metric label="自动续写" value="未设置" /></div><Tabs value={activeTab} onValueChange={setActiveTab} className="w-full"><TabsList className="rounded-none bg-transparent border-b w-full justify-start gap-8 h-12"><TabsTrigger value="outline" className="rounded-none px-0 data-[state=active]:border-b-2 data-[state=active]:border-foreground">章节大纲</TabsTrigger><TabsTrigger value="settings" className="rounded-none px-0 data-[state=active]:border-b-2 data-[state=active]:border-foreground">世界与人物</TabsTrigger><TabsTrigger value="chapters" className="rounded-none px-0 data-[state=active]:border-b-2 data-[state=active]:border-foreground">正文编辑</TabsTrigger><TabsTrigger value="trends" className="rounded-none px-0 data-[state=active]:border-b-2 data-[state=active]:border-foreground">趋势参考</TabsTrigger><TabsTrigger value="versions" className="rounded-none px-0 data-[state=active]:border-b-2 data-[state=active]:border-foreground">版本档案</TabsTrigger><TabsTrigger value="schedule" className="rounded-none px-0 data-[state=active]:border-b-2 data-[state=active]:border-foreground">续写计划</TabsTrigger></TabsList><TabsContent value="outline" className="pt-6"><div className="grid gap-6 lg:grid-cols-[1fr_330px]"><Card className="rounded-none"><CardHeader className="flex-row items-center justify-between"><CardTitle className="font-display text-2xl">全书结构提案</CardTitle><Badge className="rounded-none bg-accent text-accent-foreground">AI 生成</Badge></CardHeader><CardContent><Textarea className="min-h-[360px] leading-7" placeholder="输入故事方向，例如：女主在现代都市中发现……" value={outline} onChange={e => { setOutline(e.target.value); setDocs({ ...docs, outline: e.target.value }); }} /><div className="mt-4 flex flex-wrap gap-3"><Button className="rounded-none" disabled={generateOutline.isPending} onClick={handleGenerateOutline}><Wand2 className="mr-2 h-4 w-4" />生成 30 章提案</Button><Button variant="outline" className="rounded-none" disabled={!outline} onClick={() => saveVersion.mutate({ projectId: project.id, entityType: "outline", entityId: 0, label: "大纲快照", content: outline })}>归档版本</Button><Input className="max-w-sm rounded-none" placeholder="补充故事方向" value={direction} onChange={e => setDirection(e.target.value)} /></div></CardContent></Card><Card className="rounded-none bg-[#171613] text-[#f5f1e9] border-[#171613]"><CardHeader><CardTitle className="font-display text-2xl">编辑提示</CardTitle></CardHeader><CardContent className="text-sm leading-7 text-[#f5f1e9]/70">先让 AI 生成结构，再由你调整冲突密度、人物动机和每章结尾。满意后可将大纲复制进版本档案。</CardContent></Card></div></TabsContent><TabsContent value="settings" className="pt-6"><Card className="rounded-none"><CardHeader><CardTitle className="font-display text-2xl">策划文档</CardTitle></CardHeader><CardContent className="grid gap-5 md:grid-cols-2"><Field label="世界背景" value={docs.worldSetting} onChange={value => setDocs({ ...docs, worldSetting: value })} /><Field label="人物设定" value={docs.characters} onChange={value => setDocs({ ...docs, characters: value })} /><Field label="核心冲突" value={docs.conflicts} onChange={value => setDocs({ ...docs, conflicts: value })} /><Field label="风格指令" value={docs.styleGuide} onChange={value => setDocs({ ...docs, styleGuide: value })} /><Button className="rounded-none md:col-span-2 w-fit" onClick={() => saveDocs.mutate({ projectId: project.id, ...docs, outline })}>保存策划文档</Button></CardContent></Card></TabsContent><TabsContent value="chapters" className="pt-6"><div className="grid gap-6 lg:grid-cols-[230px_1fr]"><div className="space-y-2">{chapters.map((chapter: any) => <button key={chapter.id} onClick={() => setActiveChapter(chapter)} className={`w-full text-left p-3 border ${current?.id === chapter.id ? "bg-foreground text-background" : "bg-card"}`}><p className="text-[10px] uppercase tracking-[.2em] opacity-60">Chapter {String(chapter.chapterNumber).padStart(2, "0")}</p><p className="font-display mt-2">{chapter.title}</p></button>)}{!chapters.length && <p className="text-sm text-muted-foreground leading-6">生成大纲后，在这里逐章建立正文。</p>}</div><Card className="rounded-none"><CardHeader><CardTitle className="font-display text-2xl">正文生成器</CardTitle></CardHeader><CardContent><div className="grid gap-3 md:grid-cols-2 mb-4"><Input placeholder="章节标题" value={current?.title ?? ""} onChange={e => setActiveChapter({ ...(current ?? {}), title: e.target.value })} /><Input placeholder="目标字数" type="number" value={current?.targetWords ?? 3000} onChange={e => setActiveChapter({ ...(current ?? {}), targetWords: Number(e.target.value) })} /></div><Textarea className="min-h-[130px] mb-4" placeholder="本章大纲与场景目标" value={current?.outline ?? ""} onChange={e => setActiveChapter({ ...(current ?? {}), outline: e.target.value })} /><Textarea className="min-h-[330px] leading-8 font-serif" placeholder="生成后的正文将在这里呈现，也可以直接手动修改。" value={current?.body ?? ""} onChange={e => setActiveChapter({ ...(current ?? {}), body: e.target.value })} /><div className="mt-4 flex flex-wrap gap-3"><Button className="rounded-none" disabled={generateChapter.isPending} onClick={() => generateChapter.mutate({ projectId: project.id, chapterId: current?.id, chapterNumber: current?.chapterNumber ?? chapters.length + 1, title: current?.title || `第${chapters.length + 1}章`, outline: current?.outline || "推进主线冲突", targetWords: current?.targetWords ?? 3000, style })}><Sparkles className="mr-2 h-4 w-4" />生成本章正文</Button><Input className="min-w-[280px] rounded-none" value={style} onChange={e => setStyle(e.target.value)} placeholder="风格指令" /><Button variant="outline" className="rounded-none" disabled={!current?.id} onClick={() => current && saveChapter.mutate({ id: current.id, title: current.title, outline: current.outline, body: current.body, targetWords: current.targetWords })}>保存正文</Button></div></CardContent></Card></div></TabsContent><TabsContent value="trends" className="pt-6"><Card className="rounded-none"><CardHeader className="flex-row justify-between"><div><CardTitle className="font-display text-2xl">题材趋势库</CardTitle><p className="text-sm text-muted-foreground mt-2">手动标签与多平台公开观察统一进入表格，可按平台、题材和可信度筛选。</p></div><Clock3 className="h-5 w-5 text-muted-foreground" /></CardHeader><CardContent><div className="flex flex-wrap gap-3 mb-6"><Input className="rounded-none max-w-[180px]" placeholder="标签，例如：都市" value={newTrend.label} onChange={e => setNewTrend({ ...newTrend, label: e.target.value })} /><Input className="rounded-none max-w-[180px]" placeholder="分类" value={newTrend.category} onChange={e => setNewTrend({ ...newTrend, category: e.target.value })} /><Input className="rounded-none max-w-[220px]" placeholder="趋势备注" value={newTrend.note} onChange={e => setNewTrend({ ...newTrend, note: e.target.value })} /><Button className="rounded-none" disabled={!newTrend.label || !newTrend.category} onClick={() => createTrend.mutate(newTrend)}><Plus className="mr-2 h-4 w-4" />加入标签</Button></div><div className="mb-6 border-l-2 border-accent pl-4 text-xs leading-6 text-muted-foreground">公开来源包括番茄分类/首页观察、抖音公开搜索和 B 站公开视频搜索；来源与局限已在表格中标注。</div><TrendTable trends={trends} onEdit={trend => { const label = window.prompt("标签名称", trend.label) ?? trend.label; const category = window.prompt("标签分类", trend.category) ?? trend.category; updateTrend.mutate({ id: trend.id, label, category, heat: trend.heat, note: trend.note }); }} onDelete={trend => removeTrend.mutate({ id: trend.id })} /></CardContent></Card></TabsContent><TabsContent value="versions" className="pt-6"><Card className="rounded-none"><CardHeader><CardTitle className="font-display text-2xl">版本档案</CardTitle></CardHeader><CardContent><div className="grid gap-4 lg:grid-cols-[280px_1fr]"> <div className="space-y-2">{versions.data?.map(version => <button key={version.id} onClick={() => setSelectedVersion(version)} className={`w-full text-left border p-3 ${selectedVersion?.id === version.id ? "bg-foreground text-background" : "bg-card"}`}><p className="text-sm font-semibold">{version.label}</p><p className="text-xs opacity-60 mt-1">{new Date(version.createdAt).toLocaleString()}</p></button>)}{!versions.data?.length && <p className="text-sm text-muted-foreground">生成或归档内容后，这里会形成可回看的版本时间线。</p>}</div><div className="border p-5 min-h-[220px]"><p className="text-xs uppercase tracking-[.2em] text-muted-foreground mb-4">{selectedVersion?.label ?? "选择一个版本"}</p><pre className="whitespace-pre-wrap font-sans text-sm leading-7">{selectedVersion?.content ?? ""}</pre>{selectedVersion && <div className="mt-5 flex flex-wrap gap-3"><Button className="rounded-none" onClick={() => rollbackVersion.mutate({ versionId: selectedVersion.id })}>持久化回滚</Button><Button variant="outline" className="rounded-none" onClick={() => { if (versionEntityType === "outline") setOutline(selectedVersion.content); else if (current) setActiveChapter({ ...current, body: selectedVersion.content }); toast.success("版本已恢复到编辑器"); }}>恢复到编辑器</Button><Button variant="ghost" className="rounded-none" onClick={() => setCompareVersion(selectedVersion)}>设为对比版本</Button></div>}{compareVersion && selectedVersion && compareVersion.id !== selectedVersion.id && <div className="mt-6 grid gap-4 md:grid-cols-2 border-t pt-5"><div><p className="text-xs uppercase tracking-[.2em] text-muted-foreground mb-2">当前选择</p><pre className="whitespace-pre-wrap font-sans text-sm leading-7">{selectedVersion.content}</pre></div><div><p className="text-xs uppercase tracking-[.2em] text-muted-foreground mb-2">对比版本</p><pre className="whitespace-pre-wrap font-sans text-sm leading-7">{compareVersion.content}</pre></div></div>}</div></div></CardContent></Card></TabsContent><TabsContent value="schedule" className="pt-6"><Card className="rounded-none max-w-2xl"><CardHeader><CardTitle className="font-display text-2xl">自动续写计划</CardTitle></CardHeader><CardContent><p className="text-sm text-muted-foreground leading-7 mb-5">后台任务会按计划生成下一章，不要求浏览器保持打开。使用 UTC 六字段表达式，例如每天 22:00：<code>0 0 22 * * *</code>。</p><div className="flex gap-3"><Input className="rounded-none" value={cronExpression} onChange={e => setCronExpression(e.target.value)} /><Button className="rounded-none" onClick={() => scheduleCreate.mutate({ projectId: project.id, cronExpression })}><Clock3 className="mr-2 h-4 w-4" />创建计划</Button></div><div className="mt-5 border-l-2 border-accent pl-4 text-xs text-muted-foreground">每次完成后，系统会在通知中心写入“自动续写已完成”的站内通知。</div><div className="mt-6 space-y-3">{schedules.data?.filter((schedule: any) => schedule.projectId === project.id).map((schedule: any) => <div key={schedule.id} className="flex flex-col gap-3 border p-4 md:flex-row md:items-center md:justify-between"><div><p className="font-mono text-xs tracking-[.12em]">{schedule.cronExpression}</p><p className="mt-2 text-xs text-muted-foreground">{schedule.enabled ? "运行中" : "已暂停"}{schedule.lastRunAt ? ` · 上次成功 ${new Date(schedule.lastRunAt).toLocaleString()}` : " · 尚未成功运行"}</p>{schedule.lastError && <p className="mt-1 text-xs text-destructive">上次错误：任务未完成，请稍后重试。</p>}</div><div className="flex gap-2"><Button variant="outline" className="rounded-none" onClick={() => scheduleSetEnabled.mutate({ id: schedule.id, enabled: !schedule.enabled })}>{schedule.enabled ? "暂停" : "恢复"}</Button><Button variant="ghost" className="rounded-none text-destructive" onClick={() => scheduleRemove.mutate({ id: schedule.id })}>删除计划</Button></div></div>)}{!schedules.data?.some((schedule: any) => schedule.projectId === project.id) && <p className="text-sm text-muted-foreground">当前项目还没有续写计划。</p>}</div></CardContent></Card></TabsContent></Tabs></div>;
}
function Field({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <label className="space-y-2"><span className="text-[10px] uppercase tracking-[.22em] text-muted-foreground">{label}</span><Textarea className="min-h-[150px] rounded-none" value={value} onChange={e => onChange(e.target.value)} /></label> }
function Metric({ label, value }: { label: string; value: string }) { return <div className="bg-card p-5"><p className="text-[10px] uppercase tracking-[.2em] text-muted-foreground">{label}</p><p className="font-display text-2xl mt-3">{value}</p></div> }
