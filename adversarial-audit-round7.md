# Novel Forge 第七轮顺序验收与最终对抗性审查报告

## 验收范围

本轮按顺序检查了匿名公开页面、开发预览 HMR、认证入口、工作台与 AI 功能代码级接线、题材趋势库深链接、生产构建以及前面所有安全和可靠性修复。由于沙箱浏览器无法稳定通过 Manus 登录的人机验证，登录后的真实点击和真实 LLM 生成被明确标记为未实测，未尝试绕过认证。

## 验收结论

匿名公开页面可以正常渲染，真实浏览器控制台未出现新的 Vite WebSocket 或 React 启动错误。开发服务重启后 HMR 使用 WSS/443 配置并正常连接。点击“进入编辑室”能够正确跳转到 Manus OAuth 登录入口；认证页之后的登录操作因 CAPTCHA 阻塞而未继续执行。

工作台和 AI 功能完成代码级核对。章节大纲、章节正文、世界背景、人物设定、核心冲突、风格指令和手动续写均有明确前后端接线；procedure 使用项目归属校验、输入长度限制、持久化配额和并发锁。手动续写的历史 Heartbeat 回调返回 `manual-only`，不会调用 LLM、写入章节或创建通知。

匿名访问 `#trends` 会安全保持公开登录入口，不泄露项目数据；趋势表格、侧边栏 hash 导航、项目深链接和无效项目回退均由现有回归测试覆盖。完整 `pnpm check`、Vitest 20 个测试文件共 59 个测试、以及 `pnpm build` 均通过。

## 最终对抗性审查结果

| 审查领域 | 结果 | 说明 |
|---|---|---|
| 跨用户/跨项目访问 | 通过 | 受保护 procedure 使用当前用户和项目归属条件 |
| AI 配额与并发 | 通过 | 生成入口统一申请并释放持久化锁 |
| 自动续写残留 | 通过 | 创建/恢复被阻止，旧回调为 `manual-only` |
| 输入与资源边界 | 通过 | 字段枚举、长度、章节目标字数和方向校验均存在 |
| HMR 与生产启动 | 通过 | WSS/443 生效，生产构建显式 production |
| 导航与深链接 | 通过 | hash 白名单、project 校验、趋势入口和回退逻辑已有测试 |
| 危险前端模式 | 通过 | 未发现动态执行或新增敏感信息日志；现有图表组件的静态样式注入未发现用户输入路径 |
| 真实登录/真实模型调用 | 未实测 | 沙箱 CAPTCHA 阻塞；需普通浏览器手动验收 |

## 残余边界

最终仍建议在普通浏览器完成一次登录后验收：打开项目、点击四处 AI 入口、审核并保存生成结果、点击手动续写，以及刷新项目深链接。真实 staging 多实例并发压测也尚未执行。上述边界不代表代码级检查失败，而是外部认证、真实模型和多实例环境无法由当前沙箱可靠替代。
