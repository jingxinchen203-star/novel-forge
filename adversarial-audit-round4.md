# Novel Forge 第四轮对抗性审查报告

审查范围覆盖项目列表原生入口、selectedId 与 workspace 查询同步、URL hash/query 深链接、项目归属校验、生成配额与锁、趋势表格筛选、AI 简介与故事方向输入，以及浏览器运行时错误。

## 发现与修复

本轮确认项目卡片点击逻辑本身可触发，但项目选择只保存在 React 状态中。刷新页面或复制链接后，系统会回到首个项目；攻击者还可以构造不存在的 `project` 参数，使前端发起无效的 `workspace.get` 请求并显示错误。该问题属于中风险可靠性与信息暴露边界，不构成跨用户数据读取，因为后端仍强制执行项目归属校验。

修复后，项目入口写入受保护的 URL `project` 参数并保留现有查询参数，例如 `from_webdev=1`；页面初始化时读取该参数，只有当 ID 存在于当前用户项目列表时才使用，否则安全回退到首个可访问项目或空状态。新增了合法项目、无效项目、空项目列表和查询参数保留测试。

## 已核验安全项

项目、章节、版本、趋势、续写计划和通知 mutation 均使用受保护 procedure；资源 mutation 继续按当前用户 ID 过滤。AI 简介、生成大纲和正文生成继续执行输入长度限制、Origin 防护、项目归属校验、持久化配额和 finally 锁释放。静态扫描未发现动态执行、密钥日志或新增未保护副作用入口。近期浏览器日志未出现新的 ResizeObserver、未捕获异常或 workspace 请求错误。

## 验证结果

`pnpm check`、`pnpm test` 和 `pnpm build` 均通过；共 18 个测试文件、52 个测试通过。审查仍未替代 staging 多实例并发压测，也未覆盖所有真实外部 Heartbeat 失败组合。生产发布前应执行真实登录、项目切换、刷新恢复、越权 URL 和生成并发验证。
