# Novel Forge 第三轮对抗性审查报告

**审查对象**：Novel Forge 当前版本。审查重点包括题材选择器、项目保存校验、侧边栏 hash 导航、`projects.optimizeSynopsis`、持久化生成配额、资源归属、Origin/CSRF、错误脱敏和新增功能回归。

## 结论摘要

本轮发现一项**高风险资源耗尽缺口**：未保存项目的 AI 简介生成原先没有进入 `generation_usage` 配额与并发锁路径，已登录用户可以反复触发模型调用而不受持久化窗口限制。该问题已修复，未保存项目现在使用 `projectId = 0` 的用户级保留桶，并在成功、失败和限流路径中统一释放锁。

同时发现一项**中风险可靠性/提示边界问题**：新建项目表单原先把粗略想法和最终简介共用同一字段，AI 回写会覆盖作者的原始输入。现在已拆为“粗略想法”和“最终简介”两个独立字段，并由 `SynopsisFields` 实际渲染；AI 只回填最终简介，作者可以在保存前继续人工修改，创建 payload 只提交最终简介到 `synopsis`。

修复后，`pnpm check` 通过，Vitest **16 个测试文件、44 个测试通过**，`pnpm build` 通过。静态扫描未发现项目代码中的敏感凭据日志、动态执行或未经审查的 HTML 注入入口；图表组件中的 `dangerouslySetInnerHTML` 仅用于受控样式变量生成，仍建议后续保持其输入来源为内部配置。

## 风险与处置

| 风险等级 | 问题 | 证据与影响 | 处置状态 |
|---|---|---|---|
| 高 | 未保存项目简介生成绕过持久化配额/锁 | `optimizeSynopsis` 在无 `projectId` 时原先跳过 `reserveGenerationSlot`，可造成模型资源耗尽 | 已修复；统一使用用户级桶 `0`，并新增回归测试 |
| 中 | 粗略想法与最终简介共用字段 | AI 优化会覆盖用户原始想法，降低可追溯性并增加重复提交/提示污染风险 | 已修复；新增独立最终简介编辑区 |
| 中 | 真实登录态交互未完成 | 当前浏览器验证仍受 CAPTCHA 阻塞，无法在真实登录会话中完成点击、生成和保存闭环 | 未实测；需用户在可登录环境手动验证 |
| 低 | 题材选项不是番茄后台完整分类 | 选项来自公开页面观察样本和常用主类，不应被解释为平台实时全量榜单 | 已记录边界；保留自定义输入 |
| 低 | 生产 bundle 存在大 chunk 警告 | 构建提示主 JS 超过 500 kB，当前不影响正确性，但可能影响首屏性能 | 未修复；可后续做路由/组件拆包 |

## 已确认的安全控制

所有受保护 mutation 经过统一用户身份和可信 Origin 检查；新增回归测试确认跨源 `optimizeSynopsis` 在消耗配额和调用 LLM 之前被拒绝。保存项目、趋势、章节、版本、通知和续写计划的数据库写入均使用当前用户条件或先执行项目归属校验。AI 输入沿用字段级长度限制，模型异常统一转为 `ai_generation_failed`，不向客户端透传供应商内部错误。

侧边栏使用白名单 hash 目标，未知目标回退到 `overview`；无项目时题材趋势库、版本档案和续写计划有独立渲染分支。题材选择器允许搜索公开样本，也允许自定义输入；新建项目默认题材为空，未选择或填写题材时不能保存。

## 验证范围与限制

本轮执行了静态代码读取、危险模式扫描、类型检查、完整 Vitest 和生产构建。测试覆盖未保存/已保存简介优化、跨源拒绝、配额桶释放、LLM 失败、限流、题材选项渲染、双字段组件实际渲染与保存 payload、以及 hash 导航分支。测试使用 mock LLM 和确定性组件渲染，不代表真实模型供应商、真实多实例数据库竞争或真实浏览器登录态的最终验收。

下一步最有价值的验证是在可通过 CAPTCHA 的真实账号环境中，创建一部新小说，分别测试预设题材、自定义题材、AI 优化简介后人工修改、保存项目、进入趋势/版本/续写三个侧边栏模块，并观察通知和生成配额状态。对于正式上线前的高并发场景，还应在 staging 执行多请求、多实例的生成锁竞争测试。
