# Novel Forge 第九轮对抗性审查报告

## 审查结论

本轮审查发现并修复了一个高风险问题，以及三个中等风险的可靠性/透明度问题。修复后未发现新的项目归属绕过、跨源 mutation、手动续写回归、生产构建空白页或移动端首屏遮挡问题。

| 等级 | 问题 | 处理结果 |
|---|---|---|
| 高 | 开发调试网络日志会原样保存 Authorization、Cookie 等请求头，认证令牌可能落盘 | 已在服务端落盘前递归脱敏请求头和敏感查询参数，并清理旧本地日志；新增回归测试 |
| 中 | 差异高亮分词会丢失空白，英文文本可能显示为粘连文本 | 已将连续空白纳入 diff token，保留英文和混排间距 |
| 中 | 编辑内容变化后顶部仍显示“已保存”，容易误解为已同步服务器 | 已区分“本机草稿已保存”“离线草稿已保存”“保存中”和“已保存” |
| 中 | 手动趋势刷新可重复触发 AI 调用，且按钮名称可能暗示实时平台榜单 | 已增加基于数据库采集时间的 10 分钟冷却，并改为“手动生成趋势建议”；文案明确 AI 建议不等同实时榜单 |

## 安全边界复核

所有受保护的项目、章节、文档、版本和趋势 mutation 仍通过当前用户身份校验。趋势手动刷新只删除并替换当前用户的自动趋势快照，不触碰其他用户的手动标签；每周 cron 回调只接受 cron 会话，并写入项目所有者的自动快照。手动续写仍由用户点击触发，历史 continuation 回调保持 manual-only，不会调用章节生成。

登录回跳仍限制为同源、以 `/` 开头的相对地址；Origin/CSRF middleware 仍拒绝跨源 mutation。HMR 仍采用公开预览所需的 WSS/443 配置，生产构建继续显式使用 `NODE_ENV=production`，未恢复 React vendor 手动拆包。

## 周期任务边界

每周趋势任务当前状态为 active，时区为 `Asia/Shanghai`，配置为每周一 01:00。任务说明要求研究公开可核验来源，并通过 `/api/scheduled/refreshTrends` 回写；它不得调用续写接口。历史开发日志中曾出现一次 `Invalid session cookie`，当前回调已将无效会话安全返回 403；下一次正式任务运行仍应观察是否成功回写，以完成真实部署环境验收。匿名或普通浏览器请求不能调用该回调。

## 验证结果

`pnpm check` 通过；21 个测试文件共 66 个测试通过；生产构建通过；`git diff --check` 通过；桌面 1280px 和移动视口截图未发现首页标题、Editorial Loop、编辑器或趋势入口的明显遮挡。技能包 `novel-forge-editorial-workflow` 已更新并通过 `quick_validate.py`。

## 仍需用户环境验证

真实 OAuth、AI provider 调用、手机端底部 Drawer、离线恢复、下载行为和周期任务下一次真实运行仍需在用户常用浏览器或正式任务环境中验收。手动趋势按钮生成的是服务端 AI 研究建议，不应被解释为实时抓取平台榜单；每周任务才负责公开网页研究更新。

## 总体结论

修复后的版本适合继续迭代，没有发现需要立即回滚的高危生产缺陷。建议下一轮优先增加差异高亮的“仅新增/仅删除”视图、趋势任务运行历史和服务端草稿备份。
