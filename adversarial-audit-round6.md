# Novel Forge 第六轮对抗性审查报告

## 审查范围

本轮针对“手动续写与分区 AI 生成”版本进行全量复核，覆盖 `workspace.generateOutline`、`workspace.generateChapter`、新增的 `workspace.generateDocument` 与 `workspace.continueChapter`，以及历史 Heartbeat 回调、续写计划路由、前端编辑器按钮、AI 结果回填、策划文档保存和生产构建。

## 结论摘要

本轮未发现新的高危越权、跨项目访问、配额绕过、未授权自动生成或敏感错误透传问题。新增 AI procedure 均先验证项目归属，再申请持久化生成配额与并发锁，并在 `finally` 中释放锁。手动续写只允许显式调用；历史 Heartbeat 回调在认证后返回 `manual-only`，不再调用 LLM、不再插入章节或通知。

审查中发现两项低风险一致性问题并已修复。第一，无项目状态的续写计划说明仍使用“自动续写”旧文案；第二，生成策划文档期间保存按钮仍可提交旧状态，存在用户误保存旧内容的竞态。现已统一改为手动触发说明，并在 AI 文档生成进行中禁用“保存策划文档”。

## 关键核对结果

| 检查项 | 结果 | 证据或处理 |
|---|---|---|
| 项目与章节归属 | 通过 | `requireProject`、`requireChapter` 均使用当前用户与项目条件 |
| 文档生成字段边界 | 通过 | `field` 使用固定枚举，仅允许四个策划字段 |
| 输入长度与目标字数 | 通过 | 复用 `TEXT_LIMITS`，章节目标字数限制为 500–20,000 |
| 配额与并发 | 通过 | 新 procedure 统一调用 `reserveGenerationSlot` 与 `releasePersistentGenerationLock` |
| 重复续写 | 通过 | 计算下一章编号后检查同项目同用户的既有章节 |
| 自动续写残留 | 通过 | 创建/恢复计划被阻止；历史 Heartbeat 返回 `manual-only` |
| AI 结果写入 | 通过 | 生成结果先回填编辑器；正文续写生成后由用户审核并保存 |
| 错误处理 | 通过 | LLM helper 统一返回固定 `ai_generation_failed`，前端只显示安全错误 |
| 前端竞态 | 已修复 | AI 文档生成期间禁用策划文档保存按钮 |
| 公开/空状态文案 | 已修复 | 独立续写计划面板和项目指标统一显示手动触发策略 |

## 验证证据

`pnpm check` 通过；完整 Vitest 测试通过，共 20 个测试文件、59 个测试；`pnpm build` 通过，生产构建继续使用显式 `NODE_ENV=production` 和稳定的 React vendor 处理。静态扫描未发现动态执行、敏感环境变量日志或新的危险 HTML 注入路径。

## 残余边界

本轮没有执行真实外部 LLM 生成、真实登录后的浏览器点击和 staging 多实例并发压测，因此不能替代部署后的验收。真实环境仍应验证：点击 AI 续写后生成下一章、生成策划字段后保存、生成期间按钮禁用、旧计划删除，以及并发点击时只有一个请求获得生成锁。公开页面和 OAuth/CAPTCHA 的真实登录状态也应由用户在普通浏览器中完成最终验收。
